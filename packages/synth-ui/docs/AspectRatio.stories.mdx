import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { AspectRatio } from '../src'
import { CoverHorizontalImage, CoverVerticalImage } from './utils'

<Meta title="synth-ui|Layout/AspectRatio" component={AspectRatio} />

# AspectRatio

The `<AspectRatio>` component is used to render content according to a specific aspect ratio.

<Preview withToolbar>
  <Story inline name="intro">
    <AspectRatio style={{ width: 'calc(100% - 15px)' }}>
      <CoverHorizontalImage />
    </AspectRatio>
  </Story>
  <Story inline name="16:9">
    <AspectRatio ratio="16/9" style={{ width: 'calc(100% - 15px)' }}>
      <CoverVerticalImage />
    </AspectRatio>
  </Story>
  <Story inline name=".5">
    <AspectRatio ratio=".5" style={{ width: 'calc(100% - 15px)' }}>
      <CoverHorizontalImage />
    </AspectRatio>
  </Story>
  <Story inline name="1/1.25">
    <AspectRatio ratio="1/1.25" style={{ width: 'calc(100% - 15px)' }}>
      <CoverVerticalImage />
    </AspectRatio>
  </Story>
</Preview>

## Props

<Props of={AspectRatio} />
