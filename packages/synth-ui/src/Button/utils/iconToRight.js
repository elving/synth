import { BUTTON_ICON_POSITION_RIGHT } from '../constants'

/**
 * @param {import('@beatgig/synth-ui').ButtonIconPosition} position
 * @returns {boolean}
 */
const iconToRight = (position) => position === BUTTON_ICON_POSITION_RIGHT

export default iconToRight
