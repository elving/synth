import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'
import { Button, Card, FormField, Input } from '@beatgig/synth-ui'

<Meta title="Components/FormField" component={FormField} />

# FormField

The `<FormField>` component is used to group form elements under one label.

<Preview>
  <Story name="default">
    <div style={{ width: 320 }}>
      <Card>
        <FormField label="Email" fullWidth>
          <Input placeholder="email@example.com" />
        </FormField>
        <FormRow>
          <Button>Submit</Button>
        </FormRow>
      </Card>
    </div>
  </Story>
</Preview>

Form elements whitin a `<FormField>` could be displayed inline by using the `inline` prop.

<Preview>
  <Story name="inline">
    <div style={{ width: 320 }}>
      <Card>
        <FormField label="Email" inline>
          <Input placeholder="email@example.com" />
        </FormField>
        <FormRow>
          <Button>Submit</Button>
        </FormRow>
      </Card>
    </div>
  </Story>
</Preview>

You can provide a hint or brief instruction related to the form elements by using the `hint` prop.

<Preview>
  <Story name="hint">
    <div style={{ width: 320 }}>
      <Card>
        <FormField
          fullWidth
          hint="Use your email address; we'll use your email address to send you approximately 161.3 emails per day."
          label="Email"
        >
          <Input placeholder="email@example.com" />
        </FormField>
        <FormRow>
          <Button>Submit</Button>
        </FormRow>
      </Card>
    </div>
  </Story>
</Preview>

Error messages related to the form elements in the field can be displayed by using the `error` prop.

<Preview>
  <Story name="hint">
    <div style={{ width: 320 }}>
      <Card>
        <FormField
          error="There was an error, please try again."
          fullWidth
          hint="We'll use your email to identify you."
          label="Email"
        >
          <Input placeholder="email@example.com" />
        </FormField>
        <FormRow>
          <Button>Submit</Button>
        </FormRow>
      </Card>
    </div>  
  </Story>
</Preview>


## Props 🎛️

<Props of={FormField} />

