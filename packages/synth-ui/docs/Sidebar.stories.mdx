import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { AdminIcon, ArtistIcon, InboxIcon, Sidebar, SidebarLink } from '../src'

<Meta title="synth-ui|Sidebar" component={Sidebar} />

# Sidebar

The `<Sidebar>` and `<SidebarLink>` components are used to display a sidebar navigation.

<Preview withToolbar>
  <Story name="intro">
    <Sidebar>
      <SidebarLink icon={<AdminIcon scale={6} />}>
        Admin
      </SidebarLink>
      <SidebarLink icon={<ArtistIcon scale={6} />}>
        Discovery
      </SidebarLink>
      <SidebarLink icon={<InboxIcon scale={6} />}>
        Bookings
      </SidebarLink>
    </Sidebar>
  </Story>
</Preview>

## Props

<Props of={Sidebar} />
<Props of={SidebarLink} />