import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { SearchInput } from '../src'

<Meta title="synth-ui|SearchInput" component={SearchInput} />

# SearchInput

The `<SearchInput>` component is variant of the `<Input>` component that displays a search input.

<Preview withToolbar>
  <Story name="intro">
    <SearchInput placeholder="Search..." />
  </Story>
</Preview>

## Props

<Props of={SearchInput} />
