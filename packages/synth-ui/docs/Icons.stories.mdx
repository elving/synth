import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import {
  AdminIcon,
  ArtistIcon,
  CheckIcon,
  ChevronDownIcon,
  CloseIcon,
  Grid,
  GridItem,
  Icon,
  InboxIcon,
  LogoutIcon,
  SearchIcon,
  Spacer,
  StarEmptyIcon,
  StarFullIcon,
  StarHalfIcon,
} from '../src'

<Meta title="synth-ui|Icons" component={Icon} />

# Icon

The `<Icon>` component is used to build different icon components.

<Preview>
  <Story name="intro">
    <SearchIcon scale={5} />
  </Story>
</Preview>

## Props

| Name      | Description                                                       | Type                           | Default |
| :-------- | :---------------------------------------------------------------- | :----------------------------- | :------ |
| className | Required to properly extend styled-components.                    | `string`                       | `''`    |
| children  | The icon's markup.                                                | `React.ReactNode`              | `null`  |
| ref       | A React `ref` object that will point to the button's DOM element. | `React.Ref<HTMLButtonElement>` | `null`  |
| scale     | A scale index from the `icons` synth token.                       | `number`                       | `0`     |

## Icons

We currently use the open sourced icons from [Google Material Design](https://material.io/resources/icons).

<Preview withToolbar>
  <Story name="icons">
    <Grid columns={10}>
      <GridItem center>
        <AdminIcon scale={3} />
      </GridItem>
      <GridItem center>
        <ArtistIcon scale={3} />
      </GridItem>
      <GridItem center>
        <CheckIcon scale={3} />
      </GridItem>
      <GridItem center>
        <ChevronDownIcon scale={3} />
      </GridItem>
      <GridItem center>
        <CloseIcon scale={3} />
      </GridItem>
      <GridItem center>
        <InboxIcon scale={3} />
      </GridItem>
      <GridItem center>
        <LogoutIcon scale={3} />
      </GridItem>
      <GridItem center>
        <SearchIcon scale={3} />
      </GridItem>
      <GridItem center>
        <StarEmptyIcon scale={3} />
      </GridItem>
      <GridItem center>
        <StarHalfIcon scale={3} />
      </GridItem>
      <GridItem center>
        <StarFullIcon scale={3} />
      </GridItem>
    </Grid>
  </Story>
</Preview>
