import { Fragment } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { Background, Spacer } from '../src'
import backgroundImage from './assets/images/artist-horizontal.jpg'

<Meta title="synth-ui|Background" component={Background} />

# Background

The `<Background>` component is used to render a continer that supports all background-related CSS properties.

<Preview withToolbar>
  <Story name="intro">
    <Fragment>
      <Background
        image={backgroundImage}
        ratio="16/9"
        size="cover"
        style={{ width: '240px' }}
      />
      <Spacer inline right scale={2} />
      <Background
        color="@Oslo"
        image={backgroundImage}
        position="center center"
        ratio="16/9"
        repeat="no-repeat"
        size="50px"
        style={{ width: '240px' }}
      />
    </Fragment>
  </Story>
</Preview>

## Props

<Props of={Background} />
