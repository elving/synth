import { ICON_POSITION_RIGHT } from '../constants'

/**
 * @param {string} position
 * @returns {boolean}
 */
const iconToRight = (position) => position === ICON_POSITION_RIGHT

export default iconToRight
