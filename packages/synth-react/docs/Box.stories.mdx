import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { Box } from '../src'

<Meta title="synth-react|Primitives/Box" component={Box} />

# Box

The `<Box>` component provides an component-based interface for accessing tokens from your token configuration.

<Preview withToolbar>
  <Story name="intro">
    <Box
      backgroundColor="@BeatGig.1"
      border="input:hover"
      borderRadius="base"
      color="@Porcelain"
      padding="@spacing.3"
      opacity="modalOverlay"
      fontSize="heading"
    >
      New Box Component
    </Box>
  </Story>
</Preview>

## Props

<Props of={Box} />
