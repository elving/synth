import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { BasicConfirmationModal } from './utils'
import { ConfirmationModal } from '@beatgig/synth-ui'

<Meta title="Components/ConfirmationModal" component={ConfirmationModal} />

# ConfirmationModal

The `<ConfirmationModal>` component displays a modal used to get confirmation from the user on an action they are about to perform.

<Preview>
  <Story name="default">
    <BasicConfirmationModal />
  </Story>
</Preview>

## Props 🎛️

<Props of={ConfirmationModal} />
